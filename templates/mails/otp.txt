<h1 class="mail-h1">Verify it&rsquo;s you</h1>

<p class="mail-p">
    Hi {{ username|default:"there" }}, use the code below to
    {% if purpose == "login" %}sign in to your account{% elif purpose == "password_reset" %}reset your password{% elif purpose == "device_trust" %}confirm this device{% else %}verify your account{% endif %}.
</p>

<table role="presentation" cellpadding="0" cellspacing="0" border="0" style="margin:8px 0 24px;">
    <tr>
        <td align="center">
            <span class="mail-code">{{ code|default:obj }}</span>
        </td>
    </tr>
</table>

<p class="mail-p">
    This code expires in {{ expires_in_minutes|default:"10" }} minutes and can only be used once.
</p>

<div class="mail-divider"></div>

<p class="mail-muted">
    Didn&rsquo;t request this? You can safely ignore this email &mdash; nobody can access your
    account without the code. If this keeps happening, please
    <a href="{{ site }}" target="_blank" style="color:#1570ef;">contact support</a>.
</p>
